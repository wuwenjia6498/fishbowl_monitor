# 任务：注入灵活的 ETF 核心推荐逻辑 (v4.6 Content Update)

用户指出：不同 ETF 的推荐理由维度各不相同（有的看政策，有的看周期，有的看稀缺性），**不要强制套用统一模板**。
请严格依据上传的 PDF 文档内容，为每个 ETF 编写定制化的 `investment_logic`。

## 1. 数据注入策略 (`scripts/init_db.py`)

请更新 `CUSTOM_ETF_POOL`，在 `investment_logic` 字段中使用 **Markdown** 格式，提取 PDF 表格中 **"预估规模/流动性特征"** 和 **"核心投资逻辑与关键词"** 两列的精华内容。

**请参考以下差异化示例进行编写：**

```python
CUSTOM_ETF_POOL = [
    # --- 示例 1：科技类（强调产业链覆盖） ---
    {
        "sort_id": 101, "code": "159819.SZ", "name": "人工智能(AI)", "group": "科技 (TMT)", "etf_label": "人工智能ETF",
        "investment_logic": """
**🚀 规模与地位**
规模约233亿，同类第一。

**💡 核心逻辑**
* **全产业链覆盖：** 算力基建、大模型应用、数据要素。
* **数据要素红利：** 受益于数据资产入表及数字中国建设。
"""
    },

    # --- 示例 2：制造类（强调政策与稀缺性） ---
    {
        "sort_id": 204, "code": "159667.SZ", "name": "工业母机", "group": "高端制造", "etf_label": "工业母机ETF",
        "investment_logic": """
**💎 稀缺性**
细分赛道稀缺标的，高弹性。

**📢 政策驱动**
* **设备更新：** 受益于国家大规模设备更新政策。
* **自主可控：** 高端装备国产替代是“卡脖子”重点领域。
"""
    },

    # --- 示例 3：周期类（强调宏观属性） ---
    {
        "sort_id": 401, "code": "518880.SH", "name": "黄金", "group": "周期资源", "etf_label": "黄金ETF",
        "investment_logic": """
**🏆 亚洲最大**
规模超680亿，流动性极佳。

**🛡️ 避险与货币属性**
* **央行购金：** 全球央行持续买入，对冲美元信用风险。
* **资产压舱石：** 宏观不确定性下的核心避险资产。
"""
    },

    # --- 示例 4：消费类（强调周期反转） ---
    {
        "sort_id": 306, "code": "562900.SH", "name": "养殖", "group": "医药消费", "etf_label": "农业ETF",
        "investment_logic": """
**🐷 猪周期博弈**
* **困境反转：** 产能去化背景下，价格反弹预期强烈。
* **低费率：** 费率较低，是布局养殖复苏的低成本工具。
"""
    },
    
    # ... 请继续根据 PDF 内容，为 半导体、游戏、创新药、证券、煤炭 等补充相应的逻辑 ...
]

2. 前端渲染微调 (components/fishbowl-table.tsx)
Sheet/Dialog 组件： 保持不变。

文本渲染： 确保 Markdown 渲染组件能够正确处理 加粗 (**...**) 和 列表 (*)，使弹出的信息卡片层次分明，易于阅读。

执行指令
Python: 重写 init_db.py，将 PDF 中的所有 ETF 逻辑按上述“定制化”风格注入。

验证: 确保生成的文本包含了 PDF 中提到的具体关键词（如“设备更新”、“猪周期”、“大基金三期”等）。